#!/usr/bin/env python

import os
import sys
sys.path.insert(1, "%s")
from umdinst.wrap import capture_debugger
runprog = %s
success = capture_debugger(runprog)
if success:
   sys.exit(0)
else:
   sys.exit(1)
