#!/usr/bin/env python

import os
import sys
sys.path.insert(1, "%s")
from umdinst.wrap import capture_profile_report
runprog = %s
success = capture_profile_report(runprog)
if success:
   sys.exit(0)
else:
   sys.exit(1)
